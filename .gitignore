# --- dependencies / virtualenvs (never commit) ---
node_modules/
venv/
.venv/
__pycache__/
*.pyc
*.so

# --- secrets ---
.env
.env.*
*.pem
*.key

# --- nested working copies (these have their own .git) ---
3D-Cadastre/
InfoBhoomi_Backend_dev2/
infoBhoomi-frontedend-div2/

# --- large binaries / data dumps ---
*.mp4
*.dump
*.sql
*.zip
*.ifc

# --- os / editor ---
.DS_Store
.vscode/
*.swp

# --- generated ---
dist/
build/
.angular/

# large data kept out of git
gnd_01.csv
*.tmp

# these are source, not data — keep them
!.env.example
!agents/.env.example
!agents/grant_seed_role_perms.sql
